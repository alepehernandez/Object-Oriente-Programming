int x=100;
int search(int a[], int size, int key)

  for (int i=0; i <size, int key)
  if (key ==a[i])
  retunr i;
  
  //key not found
  retunr-1;
  
int main(int  argc, char* argc[])

const int N=10;
int arr[N]={ 100, 6,10,8,90,5,17,90,,3,125};
int key=0;

while (true{
std::cout << "Enter a number:";
std::cin >> key;

if(key == -1)
break;

std::cout <<"key =" << (key>=0)? " Found at arr[" << search(arr, N,key)<< "]";
else 
std::cout << "NOT FOUND";

std:: cout << "global x=" << x<< "\n";
int x=50;
std:: cout << "mail's x=" << x<< "\n"

int x=50;
std::cout << "main
